# TEMPLATE ONLY. Do not apply this file with real credentials and do not commit secret values.
# Create the real secrets out-of-band in velclaw-production.
#
# kubectl -n velclaw-production create secret generic velclaw-runtime \
#   --from-literal=POSTGRES_URL='...' \
#   --from-literal=VELCLAW_DEPLOY_API_TOKEN='...'
#
# kubectl -n velclaw-production create secret generic velclaw-github \
#   --from-literal=token='...'
#
# kubectl -n velclaw-production create secret generic velclaw-registry \
#   --type=kubernetes.io/dockerconfigjson \
#   --from-file=.dockerconfigjson="$HOME/.docker/config.json"
#
# Required keys:
# velclaw-runtime: POSTGRES_URL, VELCLAW_DEPLOY_API_TOKEN
# velclaw-github: token
# velclaw-registry: .dockerconfigjson
