import process from 'node:process'
import fs from 'node:fs/promises'

const API = process.env.VELCLAW_DEPLOY_API || 'http://velclaw.velclaw-production.svc.cluster.local'
const TOKEN = process.env.VELCLAW_DEPLOY_API_TOKEN || ''
const NAMESPACE = process.env.VELCLAW_RUNTIME_NAMESPACE || 'velclaw-production'
const DOMAIN = (process.env.VELCLAW_PUBLIC_DOMAIN || 'velclaw.cfd').trim().toLowerCase()
const REGISTRY = (process.env.VELCLAW_IMAGE_REGISTRY || 'ghcr.io/velclaw').replace(/\/$/, '')
const POLL_MS = Math.max(1000, Number(process.env.VELCLAW_DEPLOY_POLL_MS || 3000))
const JOB_TIMEOUT_MS = Math.max(120_000, Number(process.env.VELCLAW_K8S_JOB_TIMEOUT_MS || 900_000))
const ROLLOUT_TIMEOUT_MS = Math.max(60_000, Number(process.env.VELCLAW_K8S_ROLLOUT_TIMEOUT_MS || 600_000))
if (!TOKEN) throw new Error('VELCLAW_DEPLOY_API_TOKEN is required')
if (!/^[a-z0-9.-]+$/.test(DOMAIN)) throw new Error('Invalid VELCLAW_PUBLIC_DOMAIN')
const k8sToken = await fs.readFile('/var/run/secrets/kubernetes.io/serviceaccount/token', 'utf8').catch(() => '')
const k8sHost = process.env.KUBERNETES_SERVICE_HOST
const k8sPort = process.env.KUBERNETES_SERVICE_PORT_HTTPS || '443'
if (!k8sHost || !k8sToken) throw new Error('In-cluster Kubernetes credentials are required')
const K8S = `https://${k8sHost}:${k8sPort}`
const safe = (value, fallback = 'app') => { const result = String(value || fallback).toLowerCase().replace(/[^a-z0-9-]+/g, '-').replace(/^-+|-+$/g, ''); return (result || fallback).slice(0, 50).replace(/-+$/g, '') }
const ownerKey = job => safe(job.userId || 'legacy').slice(0, 18)
const appName = job => `vc-${safe(job.projectName)}-${ownerKey(job)}`.slice(0, 63).replace(/-+$/g, '')
const hostname = job => { const host = String(job.customDomain || `${safe(job.projectName)}-${ownerKey(job)}.${DOMAIN}`).trim().toLowerCase(); if (host !== DOMAIN && !host.endsWith(`.${DOMAIN}`)) throw new Error('Runtime hostname must remain inside the Velclaw product domain'); return host }
const imageName = job => `${REGISTRY}/${safe(job.projectName)}:${job.operation === 'rollback' && job.rollbackTargetId ? job.rollbackTargetId : job.id}`
async function control(path, options = {}) { const response = await fetch(`${API}${path}`, { ...options, headers: { authorization: `Bearer ${TOKEN}`, 'content-type': 'application/json', ...(options.headers || {}) } }); if (!response.ok) throw new Error(`${response.status} ${await response.text()}`); return response.status === 204 ? null : response.json() }
async function k8s(path, options = {}) { const response = await fetch(`${K8S}${path}`, { ...options, headers: { authorization: `Bearer ${k8sToken}`, 'content-type': 'application/json', ...(options.headers || {}) } }); if (!response.ok && response.status !== 404) throw new Error(`Kubernetes ${response.status}: ${await response.text()}`); return { status: response.status, data: response.status === 204 || response.status === 404 ? null : await response.json() } }
const pluralFor = { Secret: 'secrets', Job: 'jobs', Deployment: 'deployments', Service: 'services', Ingress: 'ingresses' }
function basePath(apiVersion, plural) { const parts = apiVersion.split('/'); return parts.length === 1 ? `/api/${parts[0]}/namespaces/${NAMESPACE}/${plural}` : `/apis/${parts[0]}/${parts[1]}/namespaces/${NAMESPACE}/${plural}` }
async function apply(resource) { const plural = pluralFor[resource.kind]; if (!plural) throw new Error(`Unsupported Kubernetes resource kind: ${resource.kind}`); const base = basePath(resource.apiVersion, plural); const existing = await k8s(`${base}/${resource.metadata.name}`); if (existing.status === 404) return k8s(base, { method: 'POST', body: JSON.stringify(resource) }); return k8s(`${base}/${resource.metadata.name}`, { method: 'PUT', body: JSON.stringify({ ...resource, metadata: { ...resource.metadata, resourceVersion: existing.data.metadata.resourceVersion } }) }) }
function buildJob(job) {
  const app = appName(job), image = imageName(job), repo = String(job.repoUrl).replace(/\.git$/i, '') + '.git', branch = String(job.branch || 'main').replace(/[^A-Za-z0-9._/-]/g, '') || 'main'
  const checkout = `if [ -n \"$GITHUB_TOKEN\" ]; then git -c http.extraheader=\"AUTHORIZATION: Bearer $GITHUB_TOKEN\" clone --depth 1 --branch '${branch}' '${repo}' /workspace/src; else git clone --depth 1 --branch '${branch}' '${repo}' /workspace/src; fi${job.commitSha ? `; cd /workspace/src; git fetch --depth 1 origin '${job.commitSha}'; git checkout --detach '${job.commitSha}'` : ''}`
  const prepare = `const fs=require('fs');const r='/workspace/src';const p=JSON.parse(fs.readFileSync(r+'/package.json','utf8'));const s=p.scripts||{};const has=f=>fs.existsSync(r+'/'+f);if(!has('Dockerfile')&&!s.build)throw new Error('Repository requires Dockerfile or package.json build script');if(!has('Dockerfile')){const m=has('pnpm-lock.yaml')?'pnpm':has('yarn.lock')?'yarn':'npm';const i=m==='pnpm'?'corepack enable && pnpm install --frozen-lockfile':m==='yarn'?'corepack enable && yarn install --immutable':has('package-lock.json')?'npm ci':'npm install';const start=s.start?m+' start':'node -e "require(\\\"http\\\").createServer((_,r)=>r.end(\\\"Velclaw app\\\")).listen(3000,\\\"0.0.0.0\\\")';fs.writeFileSync(r+'/Dockerfile','FROM node:22-alpine\\nWORKDIR /app\\nCOPY . .\\nRUN '+i+'\\nRUN '+m+' run build\\nENV NODE_ENV=production\\nENV PORT=3000\\nEXPOSE 3000\\nCMD [\\"sh\\",\\"-c\\",'+JSON.stringify(start)+']\\n')}`
  return { apiVersion:'batch/v1', kind:'Job', metadata:{name:`${app}-build-${safe(job.id).slice(0,8)}`,namespace:NAMESPACE,labels:{'app.kubernetes.io/part-of':'velclaw','velclaw.deployment':job.id}}, spec:{backoffLimit:0,ttlSecondsAfterFinished:900,activeDeadlineSeconds:Math.ceil(JOB_TIMEOUT_MS/1000),template:{metadata:{labels:{'app.kubernetes.io/name':app,'velclaw.deployment':job.id}},spec:{restartPolicy:'Never',automountServiceAccountToken:false,securityContext:{seccompProfile:{type:'RuntimeDefault'}},volumes:[{name:'source',emptyDir:{}},{name:'docker-config',secret:{secretName:'velclaw-registry',optional:false,items:[{key:'.dockerconfigjson',path:'config.json'}]}}],initContainers:[{name:'checkout',image:'alpine/git:2.47.2',command:['sh','-ec'],args:[checkout],env:[{name:'GITHUB_TOKEN',valueFrom:{secretKeyRef:{name:'velclaw-github',key:'token',optional:true}}}],volumeMounts:[{name:'source',mountPath:'/workspace'}],securityContext:{runAsUser:1000,runAsGroup:1000,runAsNonRoot:true,allowPrivilegeEscalation:false,capabilities:{drop:['ALL']}}},{name:'prepare',image:'node:22-alpine',command:['node','-e'],args:[prepare],volumeMounts:[{name:'source',mountPath:'/workspace'}],securityContext:{runAsUser:1000,runAsGroup:1000,runAsNonRoot:true,allowPrivilegeEscalation:false,capabilities:{drop:['ALL']}}}],containers:[{name:'kaniko',image:'gcr.io/kaniko-project/executor:v1.23.2-debug',args:[`--context=dir:///workspace/src`,'--dockerfile=/workspace/src/Dockerfile',`--destination=${image}`,'--cache=true','--cache-ttl=24h'],volumeMounts:[{name:'source',mountPath:'/workspace'},{name:'docker-config',mountPath:'/kaniko/.docker'}],securityContext:{runAsUser:0,allowPrivilegeEscalation:false,capabilities:{drop:['ALL']}}}]}}}}
}
async function waitForJob(jobName){const deadline=Date.now()+JOB_TIMEOUT_MS;while(Date.now()<deadline){const r=await k8s(`/apis/batch/v1/namespaces/${NAMESPACE}/jobs/${jobName}`);if(r.status===404)throw new Error('Build job disappeared');const s=r.data.status||{};if((s.succeeded||0)>0)return;if((s.failed||0)>0)throw new Error(`Kubernetes build job failed (failed=${s.failed})`);await new Promise(r=>setTimeout(r,3000))}throw new Error('Kubernetes build job timed out')}
async function waitForDeployment(app){const deadline=Date.now()+ROLLOUT_TIMEOUT_MS;while(Date.now()<deadline){const r=await k8s(`/apis/apps/v1/namespaces/${NAMESPACE}/deployments/${app}`);if(r.status===404)throw new Error('Application Deployment disappeared');const desired=Number(r.data.spec?.replicas||1),s=r.data.status||{};if(Number(s.availableReplicas||0)>=desired&&Number(s.updatedReplicas||0)>=desired&&Number(s.unavailableReplicas||0)===0)return;if((s.conditions||[]).some(c=>c.type==='Progressing'&&c.reason==='ProgressDeadlineExceeded'))throw new Error(`Rollout exceeded progress deadline: ${app}`);await new Promise(r=>setTimeout(r,3000))}throw new Error('Application rollout timed out')}
function resources(job){const app=appName(job),host=hostname(job),image=imageName(job);const env=Object.fromEntries(Object.entries(job.env||{}).filter(([k,v])=>/^[A-Za-z_][A-Za-z0-9_]{0,127}$/.test(k)&&typeof v==='string'&&!/[\r\n]/.test(v)).map(([k,v])=>[k,v.slice(0,8192)]));return[{apiVersion:'v1',kind:'Secret',metadata:{name:`${app}-env`,namespace:NAMESPACE,labels:{'velclaw.deployment':job.id}},type:'Opaque',stringData:env},{apiVersion:'apps/v1',kind:'Deployment',metadata:{name:app,namespace:NAMESPACE,labels:{'app.kubernetes.io/part-of':'velclaw','app.kubernetes.io/name':app,'velclaw.deployment':job.id}},spec:{replicas:1,revisionHistoryLimit:5,progressDeadlineSeconds:600,strategy:{type:'RollingUpdate',rollingUpdate:{maxSurge:1,maxUnavailable:0}},selector:{matchLabels:{'app.kubernetes.io/name':app}},template:{metadata:{labels:{'app.kubernetes.io/name':app,'velclaw.deployment':job.id}},spec:{imagePullSecrets:[{name:'velclaw-registry'}],containers:[{name:'web',image,imagePullPolicy:'Always',ports:[{name:'http',containerPort:3000}],envFrom:[{secretRef:{name:`${app}-env`}}],resources:{requests:{cpu:'100m',memory:'128Mi'},limits:{cpu:'1',memory:'1Gi'}},securityContext:{runAsNonRoot:true,runAsUser:10001,runAsGroup:10001,allowPrivilegeEscalation:false,capabilities:{drop:['ALL']},seccompProfile:{type:'RuntimeDefault'}},readinessProbe:{httpGet:{path:'/',port:'http'},initialDelaySeconds:10,periodSeconds:10,timeoutSeconds:3,failureThreshold:6},livenessProbe:{httpGet:{path:'/',port:'http'},initialDelaySeconds:30,periodSeconds:20,timeoutSeconds:3,failureThreshold:6}}]}}}},{apiVersion:'v1',kind:'Service',metadata:{name:app,namespace:NAMESPACE,labels:{'velclaw.deployment':job.id}},spec:{selector:{'app.kubernetes.io/name':app},ports:[{name:'http',port:80,targetPort:'http'}]}},{apiVersion:'networking.k8s.io/v1',kind:'Ingress',metadata:{name:app,namespace:NAMESPACE,annotations:{'cert-manager.io/cluster-issuer':'letsencrypt-prod','nginx.ingress.kubernetes.io/ssl-redirect':'true'},labels:{'velclaw.deployment':job.id}},spec:{ingressClassName:'nginx',tls:[{hosts:[host],secretName:`${app}-tls`}],rules:[{host,http:{paths:[{path:'/',pathType:'Prefix',backend:{service:{name:app,port:{name:'http'}}}}]}}]}}]}
async function publish(job){const logs=[...(job.logs||[]),`Kubernetes publisher started (${job.operation||'deploy'})`];try{if(job.operation==='rollback'&&job.rollbackTargetId){logs.push(`Rollback selected image ${imageName(job)}`)}else{const build=buildJob(job);await apply(build);logs.push(`Created build job ${build.metadata.name}`);await waitForJob(build.metadata.name);logs.push('Build job completed and image pushed to registry')}const app=appName(job);for(const resource of resources(job))await apply(resource);logs.push(`Applied stable runtime ${app}`);await waitForDeployment(app);logs.push(`Deployment ${app} passed readiness rollout verification`);await control(`/api/deployments/${job.id}/runtime`,{method:'POST',body:JSON.stringify({status:'ready',url:`https://${hostname(job)}`,logs:logs.slice(-500)})})}catch(error){const message=error instanceof Error?error.message:String(error);logs.push(`ERROR: ${message}`);await control(`/api/deployments/${job.id}/runtime`,{method:'POST',body:JSON.stringify({status:'failed',logs:logs.slice(-500),error:message})}).catch(()=>{})}}
console.log(`Velclaw Kubernetes publisher active: namespace=${NAMESPACE}, registry=${REGISTRY}`)
while(true){try{const {deployment}=await control('/api/deployments/claim',{method:'POST'});if(deployment)await publish(deployment)}catch(error){console.error(`[k8s-publisher] ${error instanceof Error?error.message:String(error)}`)}await new Promise(r=>setTimeout(r,POLL_MS))}
