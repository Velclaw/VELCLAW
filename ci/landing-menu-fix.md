# Landing menu fix verification

The landing page `/` must render outside the App Shell so only the landing navigation hamburger is present on mobile. The App Shell remains enabled for all non-landing routes.

Reference fix commit: e072309d893e73acd9ea866b57c96895a3f31872
